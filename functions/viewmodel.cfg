viewmodel_fov 								"60"			// Def: 54
viewmodel_presetpos 						"1"				// (Default)
viewmodel_offset_x 							"1"				// Def: 0
viewmodel_offset_y 							"1"				// Def: 0
viewmodel_offset_z 							"-1"			// Def: 0
cl_showloadout 								"1"				// (Default)
cl_use_opens_buy_menu 						"0"				// Def: 1
viewmodel_recoil 							"0"				// Def: 1

// Bobbing
cl_bob_lower_amt 							"5" 			// The amount the viewmodel lowers when running
cl_bobamt_lat 								"0.1"			// The amount the viewmodel moves side to side when running
cl_bobamt_vert 								"0.1"			// The amount the viewmodel moves up and down when running
cl_bobcycle 								"2" 			// the frequency at which the viewmodel bobs.
cl_bobup 									"0.5"    		// (Default)      